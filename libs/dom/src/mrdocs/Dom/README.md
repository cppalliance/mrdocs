# src/mrdocs/Dom/

Implementation of the document object model declared in `include/mrdocs/Dom/`:
the concrete value/object/array machinery and the lazy adapters that expose the
metadata model to templates without materializing everything up front.
