{{!-- Declare the signature of a function --}}
[source,cpp,subs=+macros]
----
{{>type-name return}}

{{#if params}}
{{name}}(
{{#each params}}
    {{>type-name type~}}
    {{#if name}} {{name}}{{/if}}
    {{~#if @last}});{{else}},{{/if}}
{{/each}}
{{else}}
{{name}}();
{{/if}}
----
