template<
{{#each params}}
    {{#if (eq kind "type")}}
    {{>type-tparam~}}
    {{else if (eq kind "non-type")}}
    {{>nontype-tparam~}}
    {{else if (eq kind "template")}}
    {{>template-tparam~}}
    {{/if}}
    {{~#unless @last}},
    {{else}}
    {{/unless}}
{{/each}}>
