# src/mrdocs/Gen/

The output generators. Each subdirectory is one output format and owns its
templates/logic: `xml/`, `html/`, `adoc/`, `hbs/` (the Handlebars-driven
generators), and `noop/` (a do-nothing generator used for extraction-only runs).
