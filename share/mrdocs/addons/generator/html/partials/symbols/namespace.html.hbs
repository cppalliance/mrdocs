{{!-- namespace --}}
<div id="{{symbol.id}}">
    <div>
        <h2>{{#if symbol.name}}Namespace {{>nested-name-specifier symbol=symbol.parent}}{{symbol.name}}{{else if symbol.parent}}Unnamed namespace{{else}}Global namespace{{/if}}</h2>
    </div>
    <div>
        {{> tranche tranche=symbol.interface label="" is-namespace=true}}
    </div>
</div>
