{{!--
    Single-page heading for the corpus-level macros section, emitted
    before the macros are appended after the global namespace. In
    multipage mode the macros get their own page instead.
--}}
{{#>markup/h level=2 id="macros"}}Macros{{/markup/h}}
