{{#if template}}{{>template/head template}}
{{/if~}}
{{#if isFriend}}friend {{/if~}}
{{~keyKind}} {{#if attributes}}{{>symbol/signature/attributes}} {{/if}}{{#if template.primary~}}
    {{!~ If the last part is a template specialization, we include links to primary template and the arguments. ~}}
    {{!~ If the primary template wasn't correctly extracted for some reason, we just print the name as usual. ~}}
    {{#if template.primary }}{{~>symbol/name (@root.mrdocs.corpus.get template.primary) ~}}{{else}}{{ name }}{{/if}}{{>template/args args=template.args ~}}
{{~else~}}
    {{>symbol/name-text symbol ~}}
{{~/if}}
{{~#if isFinal}} final{{/if~}}
{{#if bases}}

{{#each bases~}}
    {{#if @first}}
    :{{else}}
    ,{{/if~}}
    {{#unless (eq access (select (eq ../keyKind "class") "private" "public"))}} {{access}}{{/unless~}}
    {{#if isVirtual}} virtual{{/if}} {{>type/declarator type~}}
{{/each~}}
{{/if}}
{{!-- Unless we are going to show a class body ("{ ... }"), don't add a
      semicolon if the record is final, because something like `class A
      final;` is ill-formed (`final` can't be used on a non-defining
      declaration). Similarly, don't show a semicolon if there are base
      classes. --}}
{{~#if (eq extraction "see-below")}} { {{> symbol/signature/see-below}} }{{/if~}}
{{#if (or (eq extraction "see-below") (and (not isFinal) (eq (len bases) 0)))}};{{/if}}