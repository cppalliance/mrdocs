[tool.coverage.run]
branch = true
source = ["util/bootstrap/src"]
omit = ["util/bootstrap/tests/*"]

[tool.coverage.report]
show_missing = true
exclude_also = [
    "if __name__",
    "if TYPE_CHECKING",
    "pragma: no cover",
]
