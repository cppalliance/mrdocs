= Third-party dependencies

This directory contains third-party dependencies that are bundled with the mrdocs source files. These dependencies might be bundled because they are not available as packages in the other repositories or need patches to work with mrdocs.