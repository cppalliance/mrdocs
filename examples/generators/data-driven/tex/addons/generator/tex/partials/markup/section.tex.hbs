{{!--
    Section wrapper.  LaTeX has no DOM container; passthrough.  `name`
    / `id` are accepted for parity with HTML and reserved for future
    use (e.g. emitting a `\label{}` from `id`).
--}}
{{> @partial-block }}
