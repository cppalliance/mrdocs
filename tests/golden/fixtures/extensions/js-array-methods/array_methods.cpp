/// A function the JS extension renames through Array.prototype methods.
void first_function();

/// A second function, so the filtered array has more than one element.
void second_function();

/// A record the filter leaves untouched.
struct untouched_record {};
