// Header-guard suppression: the companion header wraps its body
// in an `#ifndef`/`#define`/`#endif` guard, so the corpus keeps
// `MHG_LIMIT` but drops the guard macro.
#include "macros-header-guard.hpp"
