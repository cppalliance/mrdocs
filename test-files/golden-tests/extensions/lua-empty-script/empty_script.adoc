= Reference
:mrdocs:

[#index]
== Global namespace

=== Functions

[cols="1,4"]
|===
| Name| Description
| link:#unchanged_function[`unchanged&lowbar;function`] 
| A function untouched by any extension &hyphen; the test verifies that loading an extension file that does NOT define `transform&lowbar;corpus` completes the build cleanly and leaves the corpus alone&period;
|===

[#unchanged_function]
== unchanged&lowbar;function

A function untouched by any extension &hyphen; the test verifies that loading an extension file that does NOT define `transform&lowbar;corpus` completes the build cleanly and leaves the corpus alone&period;

=== Synopsis

Declared in `&lt;empty&lowbar;script&period;cpp&gt;`

[source,cpp,subs="verbatim,replacements,macros,-callouts"]
----
void
unchanged&lowbar;function();
----


[.small]#Created with https://www.mrdocs.com[MrDocs]#
