/// A function untouched by any extension - the test verifies that
/// loading an extension file that does NOT define `transform_corpus`
/// completes the build cleanly and leaves the corpus alone.
void unchanged_function();
