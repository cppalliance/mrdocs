// Generated by docs/mrdocs/extensions/schema.js (do not edit).
// Regenerate with the schema generator; CI checks it is up to date.

The fields below are reflected from MrDocs's metadata types. In each table, object and array types link to the section that documents them.

[#symbols]
== Symbols

Base class with common properties of all symbols

[#symbols-concept]
=== concept

Info for concepts.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`constraint`
|`string`
|The concepts constraint-expression

|`template`
|`<<templateinfo,TemplateInfo>>`
|Optional. The concepts template parameters

|===

[#symbols-enumConstant]
=== enumConstant

Info for enum constants.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`initializer`
|`<<constantexprinfo,ConstantExprInfo>>`
|The initializer expression, if any

|===

[#symbols-enum]
=== enum

Metadata for an enumeration declaration.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`constants`
|`string[]`
|The members of this scope.

|`scoped`
|`boolean`
|Indicates whether this enum is scoped (e.g. enum class).

|`underlyingType`
|`<<types,Type>>`
|Optional. The underlying type of this enum, if explicitly specified.

|===

[#symbols-function]
=== function

Metadata for a function or method.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`constexpr`
|`string`
|constexpr/consteval specifier. One of: `none`, `constexpr`, `consteval`.

|`explicit`
|`<<explicitinfo,ExplicitInfo>>`
|explicit-specifier information.

|`funcClass`
|`string`
|The class of function this is One of: `normal`, `constructor`, `conversion`, `destructor`.

|`functionObjectImpl`
|`string`
|Optional. Back-reference to the function object implementation type.

|`hasOverrideAttr`
|`boolean`
|True when annotated with override.

|`hasTrailingReturn`
|`boolean`
|True when using a trailing return type.

|`isConst`
|`boolean`
|True when qualified const.

|`isDefaulted`
|`boolean`
|True when this declaration is implicitly defaulted.

|`isDeleted`
|`boolean`
|True when this declaration is deleted.

|`isDeletedAsWritten`
|`boolean`
|True when deleted as written (vs deduced).

|`isExplicitObjectMemberFunction`
|`boolean`
|True when explicit object parameter syntax is used.

|`isExplicitlyDefaulted`
|`boolean`
|True when explicitly defaulted with `= default`.

|`isFinal`
|`boolean`
|True when final-qualified.

|`isListedOnPrimary`
|`boolean`
|Whether this function is listed on its primary's page.

|`isPure`
|`boolean`
|True when the function is pure virtual.

|`isRecordMethod`
|`boolean`
|True when this is a non-static member function.

|`isVariadic`
|`boolean`
|True when the function is variadic.

|`isVirtual`
|`boolean`
|True when declared virtual (after overrides).

|`isVirtualAsWritten`
|`boolean`
|True when explicitly written virtual.

|`isVolatile`
|`boolean`
|True when qualified volatile.

|`noexcept`
|`<<noexceptinfo,NoexceptInfo>>`
|Exception specification for the function.

|`overloadedOperator`
|`string`
|Overloaded operator kind, if any. One of: `none`, `new`, `delete`, `array-new`, `array-delete`, `plus`, `minus`, `star`, `slash`, `percent`, `caret`, `amp`, `pipe`, `tilde`, `equal`, `plus-equal`, `minus-equal`, `star-equal`, `slash-equal`, `percent-equal`, `caret-equal`, `amp-equal`, `pipe-equal`, `less-less`, `greater-greater`, `less-less-equal`, `greater-greater-equal`, `exclaim`, `equal-equal`, `exclaim-equal`, `less`, `less-equal`, `greater`, `greater-equal`, `spaceship`, `amp-amp`, `pipe-pipe`, `plus-plus`, `minus-minus`, `comma`, `arrow-star`, `arrow`, `call`, `subscript`, `conditional`, `coawait`.

|`params`
|`<<param,Param[]>>`
|List of parameters.

|`refQualifier`
|`string`
|Reference qualifier on the member function, if any. One of: `none`, `l-value`, `r-value`.

|`requires`
|`string`
|Constrained requires-clause if present.

|`returnType`
|`<<types,Type>>`
|Info about the return type of this function.

|`specializations`
|`string[]`
|Specializations whose primary is this function.

|`storageClass`
|`string`
|Storage class specifier. One of: `none`, `extern`, `static`, `auto`, `register`.

|`template`
|`<<templateinfo,TemplateInfo>>`
|Optional. When present, this function is a template or specialization.

|===

[#symbols-guide]
=== guide

Info for deduction guides.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`deduced`
|`<<types,Type>>`
|The pattern for the deduced specialization.

|`explicit`
|`<<explicitinfo,ExplicitInfo>>`
|The explicit-specifier, if any.

|`params`
|`<<param,Param[]>>`
|The parameters of the deduction guide.

|`template`
|`<<templateinfo,TemplateInfo>>`
|Optional. Template head, if any.

|===

[#symbols-macro]
=== macro

Info for preprocessor macros.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`isFunctionLike`
|`boolean`
|Whether this is a function-like macro.

|`isVariadic`
|`boolean`
|Whether the macro takes a variadic argument list.

|`parameters`
|`string[]`
|The names of the macro's parameters.

|===

[#symbols-namespaceAlias]
=== namespaceAlias

Info for namespace aliases.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`aliasedSymbol`
|`<<names,IdentifierName>>`
|The aliased symbol.

|===

[#symbols-namespace]
=== namespace

Describes a namespace and its members.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`isAnonymous`
|`boolean`
|Whether this represents an unnamed namespace.

|`isInline`
|`boolean`
|Whether this declaration is inline.

|`members`
|`<<namespacetranche,NamespaceTranche>>`
|The members of this namespace.

|`usingDirectives`
|`<<names,Name[]>>`
|Namespaces nominated by using-directives.

|===

[#symbols-overloads]
=== overloads

Represents a set of function overloads.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`funcClass`
|`string`
|The class of the functions. One of: `normal`, `constructor`, `conversion`, `destructor`.

|`members`
|`string[]`
|The members of the overload set.

|`overloadedOperator`
|`string`
|The overloaded operator, if any. One of: `none`, `new`, `delete`, `array-new`, `array-delete`, `plus`, `minus`, `star`, `slash`, `percent`, `caret`, `amp`, `pipe`, `tilde`, `equal`, `plus-equal`, `minus-equal`, `star-equal`, `slash-equal`, `percent-equal`, `caret-equal`, `amp-equal`, `pipe-equal`, `less-less`, `greater-greater`, `less-less-equal`, `greater-greater-equal`, `exclaim`, `equal-equal`, `exclaim-equal`, `less`, `less-equal`, `greater`, `greater-equal`, `spaceship`, `amp-amp`, `pipe-pipe`, `plus-plus`, `minus-minus`, `comma`, `arrow-star`, `arrow`, `call`, `subscript`, `conditional`, `coawait`.

|`returnType`
|`<<types,Type>>`
|Info about the return type of these function overloads.

|===

[#symbols-record]
=== record

Metadata for struct, class, or union.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`bases`
|`<<baseinfo,BaseInfo[]>>`
|List of immediate bases.

|`deductionGuides`
|`string[]`
|Deduction guides associated with this class template.

|`derived`
|`string[]`
|List of derived classes

|`friends`
|`<<friendinfo,FriendInfo[]>>`
|List of friends.

|`interface`
|`<<recordinterface,RecordInterface>>`
|Lists of members.

|`isFinal`
|`boolean`
|Whether the class is marked `final`.

|`isFinalDestructor`
|`boolean`
|Whether the destructor is marked `final`.

|`isListedOnPrimary`
|`boolean`
|Whether this record is listed on its primary's page.

|`isTypeDef`
|`boolean`
|Whether the record originated from a typedef-style declaration.

|`keyKind`
|`string`
|Kind of record this is (class, struct, or union). One of: `struct`, `class`, `union`.

|`specializations`
|`string[]`
|Specializations whose primary is this record.

|`template`
|`<<templateinfo,TemplateInfo>>`
|Optional. When present, this record is a template or specialization.

|===

[#symbols-typedef]
=== typedef

Info for typedef and using declarations.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`isUsing`
|`boolean`
|Indicates if this is a new C++ "using"-style typedef

|`template`
|`<<templateinfo,TemplateInfo>>`
|Optional. Template information when the alias is templated.

|`type`
|`<<types,Type>>`
|The aliased type.

|===

[#symbols-using]
=== using

Info for using declarations.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`class`
|`string`
|The using declaration. One of: `normal`, `typename`, `enum`.

|`introducedName`
|`<<names,Name>>`
|The symbol being introduced.

|`shadowDeclarations`
|`string[]`
|The shadow declarations.

|===

[#symbols-variable]
=== variable

A variable.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|Declaration access. One of: `none`, `public`, `protected`, `private`.

|`anchor`
|`string`
|A legible, filesystem- and URL-safe name for this symbol.

|`attributes`
|`<<attributes,Attribute[]>>`
|The C++ attributes attached to this declaration.

|`extraction`
|`string`
|Determine why a symbol is extracted. One of: `regular`, `see-below`, `implementation-defined`, `dependency`.

|`isCopyFromInherited`
|`boolean`
|Whether this a copy of an inherited method, as produced when `inherit-base-members` is not `never`.

|`kind`
|`string`
|Kind of declaration. One of: `none`.

|`loc`
|`<<sourceinfo,SourceInfo>>`
|The source location information.

|`name`
|`string`
|The unqualified name.

|`parent`
|`string`
|The parent symbol, if any.

|`doc`
|`<<doccomment,DocComment>>`
|Optional. The extracted documentation for this declaration.

|`id`
|`string`
|The unique identifier for this symbol.

|`bitfieldWidth`
|`<<constantexprinfo,ConstantExprInfo>>`
|The width of the bitfield

|`initializer`
|`string`
|The default member initializer, if any.

|`isBitfield`
|`boolean`
|Whether the field is a bitfield

|`isConstexpr`
|`boolean`
|Whether the variable is `constexpr`.

|`isConstinit`
|`boolean`
|Whether the variable is `constinit`.

|`isInline`
|`boolean`
|Whether the variable is declared `inline`.

|`isMutable`
|`boolean`
|Whether the field is declared mutable

|`isRecordField`
|`boolean`
|True if this variable is a data member of a record.

|`isThreadLocal`
|`boolean`
|Whether the variable is `thread_local`.

|`isVariant`
|`boolean`
|Whether the field is a variant member

|`storageClass`
|`string`
|Storage class specifier applied to the variable. One of: `none`, `extern`, `static`, `auto`, `register`.

|`template`
|`<<templateinfo,TemplateInfo>>`
|Optional. The template information, if any.

|`type`
|`<<types,Type>>`
|The type of the variable

|===

[#types]
== Types

A possibly qualified type.

[#types-array]
=== array

C++ array type (bounded or unbounded).

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`bounds`
|`<<constantexprinfo,ConstantExprInfo>>`
|Optional bound; empty means unknown or dependent.

|`elementType`
|`<<types,Type>>`
|Element type held by the array.

|===

[#types-auto]
=== auto

Represents `auto` or `decltype(auto)` placeholder type.

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`constraint`
|`<<names,Name>>`
|Optional. Constraint on the auto type, if any.

|`keyword`
|`string`
|Which placeholder keyword appears (`auto` or `decltype(auto)`). One of: `auto`, `decltype-auto`.

|===

[#types-decltype]
=== decltype

`decltype(expr)` type wrapper.

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`operand`
|`string`
|Operand expression for decltype.

|===

[#types-function]
=== function

Function type with parameters, qualifiers, and noexcept info.

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`exceptionSpec`
|`<<noexceptinfo,NoexceptInfo>>`
|Exception specification information.

|`isVariadic`
|`boolean`
|True when this function type is variadic.

|`paramTypes`
|`<<types,Type[]>>`
|Parameter types in declaration order.

|`refQualifier`
|`string`
|Reference qualifier (if present). One of: `none`, `l-value`, `r-value`.

|`returnType`
|`<<types,Type>>`
|Return type, defaults to `auto` when unknown.

|===

[#types-lValueReference]
=== lValueReference

An lvalue reference type.

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`pointeeType`
|`<<types,Type>>`
|The referenced type.

|===

[#types-memberPointer]
=== memberPointer

Pointer-to-member type (object or function).

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`parentType`
|`<<types,Type>>`
|Containing class type.

|`pointeeType`
|`<<types,Type>>`
|Pointee type being referenced.

|===

[#types-named]
=== named

A type identified by name (possibly fundamental).

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`fundamentalType`
|`string`
|Optional. Fundamental kind if this named type is a built-in. One of: `void`, `nullptr`, `bool`, `char`, `signed-char`, `unsigned-char`, `char8`, `char16`, `char32`, `w-char`, `short`, `unsigned-short`, `int`, `unsigned-int`, `long`, `unsigned-long`, `long-long`, `unsigned-long-long`, `float`, `double`, `long-double`.

|`name`
|`<<names,Name>>`
|The name of the type (identifier or specialization).

|===

[#types-pointer]
=== pointer

Pointer type wrapper.

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`pointeeType`
|`<<types,Type>>`
|The pointee type, defaults to `auto` when unknown.

|===

[#types-rValueReference]
=== rValueReference

An rvalue reference type.

[cols="1,1,2"]
|===
|Field |Type |Description

|`constraints`
|`string[]`
|The constraints associated with the type

|`isConst`
|`boolean`
|The const qualifier

|`isPackExpansion`
|`boolean`
|Whether this is the pattern of a pack expansion.

|`isVolatile`
|`boolean`
|The volatile qualifier

|`kind`
|`string`
|The kind of Type this is

|`pointeeType`
|`<<types,Type>>`
|The referenced type, defaults to `auto` when unknown.

|===

[#names]
== Names

Represents a name for a named `Type`

[#names-identifier]
=== identifier

Represents an identifier

[cols="1,1,2"]
|===
|Field |Type |Description

|`identifier`
|`string`
|The unqualified name.

|`kind`
|`string`
|The kind of name this is.

|`prefix`
|`<<names,Name>>`
|Optional. The parent name info, if any.

|`id`
|`string`
|The SymbolID of the named symbol, if it exists.

|===

[#names-specialization]
=== specialization

Represents a (possibly qualified) symbol name with template arguments.

[cols="1,1,2"]
|===
|Field |Type |Description

|`identifier`
|`string`
|The unqualified name.

|`kind`
|`string`
|The kind of name this is.

|`prefix`
|`<<names,Name>>`
|Optional. The parent name info, if any.

|`id`
|`string`
|The SymbolID of the named symbol, if it exists.

|`templateArgs`
|`<<template-arguments,TArg[]>>`
|The template arguments.

|`specializationID`
|`string`
|The SymbolID of the named symbol, if it exists.

|===

[#attributes]
== Attributes

A C++ attribute attached to a symbol.

[#attributes-assume]
=== assume

The `[[assume(expression)]]` attribute (C++23).

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|`expression`
|`string`
|The assumed expression, as written.

|===

[#attributes-carriesDependency]
=== carriesDependency

The `[[carries_dependency]]` attribute (C++11): release-consume dependency propagation.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#attributes-deprecated]
=== deprecated

The `[[deprecated]]` attribute (C++14).

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|`message`
|`string`
|The deprecation message, if any.

|===

[#attributes-fallthrough]
=== fallthrough

The `[[fallthrough]]` attribute (C++17): an intentional switch fall-through.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#attributes-indeterminate]
=== indeterminate

The `[[indeterminate]]` attribute (C++26): the object has an indeterminate value if uninitialized.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#attributes-likely]
=== likely

The `[[likely]]` attribute (C++20): this path is more likely.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#attributes-maybeUnused]
=== maybeUnused

The `[[maybe_unused]]` attribute (C++17): suppresses unused-entity warnings.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#attributes-noUniqueAddress]
=== noUniqueAddress

The `[[no_unique_address]]` attribute (C++20): the member need not have a unique address.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#attributes-nodiscard]
=== nodiscard

The `[[nodiscard]]` attribute (C++17, reason added in C++20).

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|`reason`
|`string`
|The reason, if any.

|===

[#attributes-noreturn]
=== noreturn

The `[[noreturn]]` attribute (C++11): the function does not return.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#attributes-other]
=== other

An attribute mrdocs does not treat specially.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#attributes-unlikely]
=== unlikely

The `[[unlikely]]` attribute (C++20): this path is less likely.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|The kind of attribute.

|`name`
|`string`
|The attribute name as it appears in the standard form.

|`balancedTokens`
|`string[]`
|The attribute arguments as a balanced-token sequence.

|===

[#template-parameters]
== Template Parameters

Base class for a template parameter declaration.

[#template-parameters-constant]
=== constant

A constant template parameter

[cols="1,1,2"]
|===
|Field |Type |Description

|`default`
|`<<template-arguments,TArg>>`
|Optional. The default template argument, if any

|`isParameterPack`
|`boolean`
|Whether this template parameter is a parameter pack

|`kind`
|`string`
|The kind of template parameter this is

|`name`
|`string`
|The template parameters name, if any

|`type`
|`<<types,Type>>`
|Type of the non-type template parameter

|===

[#template-parameters-template]
=== template

Template template parameter.

[cols="1,1,2"]
|===
|Field |Type |Description

|`default`
|`<<template-arguments,TArg>>`
|Optional. The default template argument, if any

|`isParameterPack`
|`boolean`
|Whether this template parameter is a parameter pack

|`kind`
|`string`
|The kind of template parameter this is

|`name`
|`string`
|The template parameters name, if any

|`params`
|`<<template-parameters,TParam[]>>`
|Template parameters for the template-template parameter

|===

[#template-parameters-type]
=== type

Type template parameter.

[cols="1,1,2"]
|===
|Field |Type |Description

|`default`
|`<<template-arguments,TArg>>`
|Optional. The default template argument, if any

|`isParameterPack`
|`boolean`
|Whether this template parameter is a parameter pack

|`kind`
|`string`
|The kind of template parameter this is

|`name`
|`string`
|The template parameters name, if any

|`constraint`
|`<<names,Name>>`
|Optional. The type-constraint for the parameter, if any.

|`keyKind`
|`string`
|Keyword (class/typename) the parameter uses One of: `class`, `typename`.

|===

[#template-arguments]
== Template Arguments

Base class for any template argument.

[#template-arguments-constant]
=== constant

Non-type template argument.

[cols="1,1,2"]
|===
|Field |Type |Description

|`isPackExpansion`
|`boolean`
|Whether this template argument is a parameter expansion.

|`kind`
|`string`
|The kind of template argument this is.

|`value`
|`string`
|Template argument expression.

|===

[#template-arguments-template]
=== template

Template template argument.

[cols="1,1,2"]
|===
|Field |Type |Description

|`isPackExpansion`
|`boolean`
|Whether this template argument is a parameter expansion.

|`kind`
|`string`
|The kind of template argument this is.

|`name`
|`string`
|Name of the referenced template.

|`template`
|`string`
|SymbolID of the referenced template.

|===

[#template-arguments-type]
=== type

Type template argument.

[cols="1,1,2"]
|===
|Field |Type |Description

|`isPackExpansion`
|`boolean`
|Whether this template argument is a parameter expansion.

|`kind`
|`string`
|The kind of template argument this is.

|`type`
|`<<types,Type>>`
|Template argument type.

|===

[#documentation-blocks]
== Documentation Blocks

A piece of block content

[#documentation-blocks-admonition]
=== admonition

A block for side-notes like tips, warnings, notes

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`blocks`
|`<<documentation-blocks,Block[]>>`
|Child blocks contained within this composite block.

|`title`
|`<<documentation-inlines,Inline>>`
|Optional. Optional title for the admonition

|`admonish`
|`string`
|The kind of admonition One of: `none`, `note`, `tip`, `important`, `caution`, `warning`.

|===

[#documentation-blocks-brief]
=== brief

The brief description

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|`copiedFrom`
|`string[]`
|Names of declarations whose brief text was reused.

|===

[#documentation-blocks-code]
=== code

Preformatted source code.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`info`
|`string`
|Fence info string, e.g. "cpp"

|`literal`
|`string`
|Raw code text inside the fenced block.

|===

[#documentation-blocks-definitionList]
=== definitionList

A list of terms paired with definitions.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`items`
|`<<definitionlistitem,DefinitionListItem[]>>`
|Sequence of definition list items.

|===

[#documentation-blocks-footnoteDefinition]
=== footnoteDefinition

Text defining the content of a footnote reference.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`blocks`
|`<<documentation-blocks,Block[]>>`
|Child blocks contained within this composite block.

|`label`
|`string`
|Footnote label identifier.

|===

[#documentation-blocks-heading]
=== heading

A manually specified section heading.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|`level`
|`integer`
|Heading depth (1..6).

|===

[#documentation-blocks-list]
=== list

A list of list items

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`items`
|`<<listitem,ListItem[]>>`
|Items contained in the list.

|`listKind`
|`string`
|Display style for the list. One of: `unordered`, `ordered`.

|===

[#documentation-blocks-math]
=== math

A block of LaTeX math

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`literal`
|`string`
|Raw TeX math source

|===

[#documentation-blocks-paragraph]
=== paragraph

A sequence of text nodes.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-blocks-param]
=== param

Documentation for a function parameter

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|`direction`
|`string`
|Direction (in/out/inout) of the parameter. One of: `none`, `in`, `out`, `inout`.

|`name`
|`string`
|Name of the parameter being documented.

|===

[#documentation-blocks-postcondition]
=== postcondition

Text describing conditions guaranteed on successful exit.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-blocks-precondition]
=== precondition

Text describing required conditions before entry.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-blocks-quote]
=== quote

A quoted block of text.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`blocks`
|`<<documentation-blocks,Block[]>>`
|Child blocks contained within this composite block.

|===

[#documentation-blocks-returns]
=== returns

Documentation for a function return type

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-blocks-see]
=== see

A

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-blocks-tParam]
=== tParam

Documentation for a template parameter

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|`name`
|`string`
|Template parameter name.

|===

[#documentation-blocks-table]
=== table

A table block

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`alignments`
|`array`
|Column alignments for each table column.

|`items`
|`<<tablerow,TableRow[]>>`
|Rows that make up the table body (header first when present).

|===

[#documentation-blocks-thematicBreak]
=== thematicBreak

A horizontal thematic break separating sections.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|===

[#documentation-blocks-throws]
=== throws

Documentation for a function exception clause

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which concrete block this instance holds.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|`exception`
|`<<documentation-inlines,ReferenceInline>>`
|Exception type being described.

|===

[#documentation-inlines]
== Documentation Inlines

A Node containing a string of text.

[#documentation-inlines-code]
=== code

Inline code (monospaced) used for short snippets within text.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-inlines-copyDetails]
=== copyDetails

Documentation copied from another symbol.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`id`
|`string`
|Symbol to copy details from.

|`string`
|`string`
|Element to copy the details from

|===

[#documentation-inlines-emph]
=== emph

Emphasized text (typically rendered in italics).

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-inlines-footnoteReference]
=== footnoteReference

A reference to a symbol.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`label`
|`string`
|Footnote label that the reference points to.

|===

[#documentation-inlines-highlight]
=== highlight

Highlighted text span used to call out important words.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-inlines-image]
=== image

An image.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|`alt`
|`string`
|Alternate text when the image cannot be shown.

|`src`
|`string`
|Image source URL or path.

|===

[#documentation-inlines-lineBreak]
=== lineBreak

A hard line break that renders as ""

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|===

[#documentation-inlines-link]
=== link

A hyperlink.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|`href`
|`string`
|Destination of the hyperlink.

|===

[#documentation-inlines-math]
=== math

An inline LaTeX math expression

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`literal`
|`string`
|Raw LaTeX/TeX math content.

|===

[#documentation-inlines-reference]
=== reference

A reference to a symbol.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`id`
|`string`
|Symbol being referenced.

|`literal`
|`string`
|Display text of the reference.

|===

[#documentation-inlines-softBreak]
=== softBreak

A line break that may render as a space

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|===

[#documentation-inlines-strikethrough]
=== strikethrough

Strikethrough span to show removed or deprecated text.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-inlines-strong]
=== strong

Strong emphasis span (typically rendered in bold).

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-inlines-subscript]
=== subscript

Subscript text fragment (lowered baseline).

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-inlines-superscript]
=== superscript

Superscript text fragment (raised baseline).

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#documentation-inlines-text]
=== text

A Node containing a string of text.

[cols="1,1,2"]
|===
|Field |Type |Description

|`kind`
|`string`
|Discriminator identifying which inline variant is active.

|`literal`
|`string`
|Plain text carried by this inline node.

|===

[#other-structures]
== Other Structures

[#baseinfo]
=== BaseInfo

Metadata for a direct base.

[cols="1,1,2"]
|===
|Field |Type |Description

|`access`
|`string`
|The access specifier for the base. One of: `none`, `public`, `protected`, `private`.

|`isVirtual`
|`boolean`
|Whether the base is virtual.

|`type`
|`<<types,Type>>`
|The base type.

|===

[#constantexprinfo]
=== ConstantExprInfo

Represents an expression with a (possibly known) value

[cols="1,1,2"]
|===
|Field |Type |Description

|`written`
|`string`
|The expression, as written

|`value`
|`value`
|Optional. The expressions value, if it is known

|===

[#definitionlistitem]
=== DefinitionListItem

An item in a definition list

[cols="1,1,2"]
|===
|Field |Type |Description

|`blocks`
|`<<documentation-blocks,Block[]>>`
|Child blocks contained within this composite block.

|`term`
|`<<inlinecontainer,InlineContainer>>`
|The term being defined.

|===

[#doccomment]
=== DocComment

A processed documentation comment attached to a declaration.

[cols="1,1,2"]
|===
|Field |Type |Description

|`document`
|`<<documentation-blocks,Block[]>>`
|The list of text blocks.

|`isFunctionObject`
|`boolean`
|True if the

|`isImplementationDefined`
|`boolean`
|True if the

|`isSeeBelow`
|`boolean`
|True if the

|`brief`
|`<<documentation-blocks,BriefBlock>>`
|Optional. A brief description of the symbol.

|`exceptions`
|`<<documentation-blocks,ThrowsBlock[]>>`
|The list of exceptions.

|`footnotes`
|`<<documentation-blocks,FootnoteDefinitionBlock[]>>`
|Footnote definitions.

|`params`
|`<<documentation-blocks,ParamBlock[]>>`
|The list of parameters.

|`postconditions`
|`<<documentation-blocks,PostconditionBlock[]>>`
|The list of postconditions.

|`preconditions`
|`<<documentation-blocks,PreconditionBlock[]>>`
|The list of preconditions.

|`related`
|`<<documentation-inlines,ReferenceInline[]>>`
|The list of "related" references.

|`relates`
|`<<documentation-inlines,ReferenceInline[]>>`
|The list of "relates" references.

|`returns`
|`<<documentation-blocks,ReturnsBlock[]>>`
|The list of return type descriptions.

|`sees`
|`<<documentation-blocks,SeeBlock[]>>`
|The list of "see also" references.

|`tparams`
|`<<documentation-blocks,TParamBlock[]>>`
|The list of template parameters.

|===

[#explicitinfo]
=== ExplicitInfo

Stores only the operand of the explicit-specifier or noexcept-specifier as a string. The complete expression is not preserved at this time. This is a temporary design and may be improved in the future.

[cols="1,1,2"]
|===
|Field |Type |Description

|`implicit`
|`boolean`
|Whether an explicit-specifier was user-written.

|`kind`
|`string`
|The evaluated exception specification. One of: `false`, `true`, `dependent`.

|`operand`
|`string`
|The operand of the explicit-specifier, if any.

|===

[#friendinfo]
=== FriendInfo

Info for friend declarations.

[cols="1,1,2"]
|===
|Field |Type |Description

|`type`
|`<<types,Type>>`
|Optional. Befriended type.

|`id`
|`string`
|Befriended symbol.

|===

[#inlinecontainer]
=== InlineContainer

An internal node in the inline element tree.

[cols="1,1,2"]
|===
|Field |Type |Description

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#listitem]
=== ListItem

An item in a `ListBlock`

[cols="1,1,2"]
|===
|Field |Type |Description

|`blocks`
|`<<documentation-blocks,Block[]>>`
|Child blocks contained within this composite block.

|===

[#location]
=== Location

Source location of a symbol or entity.

[cols="1,1,2"]
|===
|Field |Type |Description

|`columnNumber`
|`integer`
|Column number within the line

|`documented`
|`boolean`
|Whether this location has documentation.

|`fullPath`
|`string`
|The full file path

|`lineNumber`
|`integer`
|Line number within the file

|`shortPath`
|`string`
|The file path relative to one of the search directories

|`sourcePath`
|`string`
|The file path relative to the source-root directory

|===

[#namespacetranche]
=== NamespaceTranche

Buckets the members that appear inside a namespace.

[cols="1,1,2"]
|===
|Field |Type |Description

|`concepts`
|`string[]`
|Concepts.

|`enums`
|`string[]`
|Enumerations.

|`functions`
|`string[]`
|Functions and overload sets.

|`guides`
|`string[]`
|Deduction guides.

|`macros`
|`string[]`
|Preprocessor macros.

|`namespaceAliases`
|`string[]`
|Namespace aliases declared here.

|`namespaces`
|`string[]`
|Nested namespaces.

|`records`
|`string[]`
|Record types (classes/structs).

|`typedefs`
|`string[]`
|Typedef or using declarations.

|`usings`
|`string[]`
|Using-declarations that introduce members.

|`variables`
|`string[]`
|Variables.

|===

[#noexceptinfo]
=== NoexceptInfo

Captures a `noexcept` specification and its evaluated form.

[cols="1,1,2"]
|===
|Field |Type |Description

|`implicit`
|`boolean`
|Whether a noexcept-specifier was user-written.

|`kind`
|`string`
|The evaluated exception specification. One of: `false`, `true`, `dependent`.

|`operand`
|`string`
|The operand of the noexcept-specifier, if any.

|===

[#param]
=== Param

Represents a single function parameter

[cols="1,1,2"]
|===
|Field |Type |Description

|`default`
|`string`
|Optional. The default argument for this parameter, if any

|`name`
|`string`
|Optional. The parameter name.

|`type`
|`<<types,Type>>`
|The type of this parameter

|===

[#recordinterface]
=== RecordInterface

The aggregated interface for a given struct, class, or union.

[cols="1,1,2"]
|===
|Field |Type |Description

|`private`
|`<<recordtranche,RecordTranche>>`
|The aggregated private interfaces.

|`protected`
|`<<recordtranche,RecordTranche>>`
|The aggregated protected interfaces.

|`public`
|`<<recordtranche,RecordTranche>>`
|The aggregated public interfaces.

|===

[#recordtranche]
=== RecordTranche

A group of members that have the same access specifier.

[cols="1,1,2"]
|===
|Field |Type |Description

|`concepts`
|`string[]`
|Member concepts.

|`enums`
|`string[]`
|Enumerations.

|`functions`
|`string[]`
|Member functions.

|`guides`
|`string[]`
|Deduction guides in the class scope.

|`namespaceAliases`
|`string[]`
|Namespace aliases declared with this access.

|`records`
|`string[]`
|Nested records.

|`staticFunctions`
|`string[]`
|Static member functions.

|`staticVariables`
|`string[]`
|Static data members.

|`typedefs`
|`string[]`
|Typedefs and using aliases.

|`usings`
|`string[]`
|Using-declarations that pull members into the class.

|`variables`
|`string[]`
|Data members.

|===

[#sourceinfo]
=== SourceInfo

Stores source information for a declaration.

[cols="1,1,2"]
|===
|Field |Type |Description

|`defLoc`
|`<<location,Location>>`
|Optional. Location where the entity was defined

|`loc`
|`<<location,Location[]>>`
|Locations where the entity was declared.

|===

[#tablecell]
=== TableCell

A cell in a table

[cols="1,1,2"]
|===
|Field |Type |Description

|`children`
|`<<documentation-inlines,Inline[]>>`
|Child inline elements contained here.

|===

[#tablerow]
=== TableRow

An item in a list

[cols="1,1,2"]
|===
|Field |Type |Description

|`cells`
|`<<tablecell,TableCell[]>>`
|Cells contained in the row.

|`isHeader`
|`boolean`
|True if this row represents a header.

|===

[#templateinfo]
=== TemplateInfo

Information about templates and specializations thereof.

[cols="1,1,2"]
|===
|Field |Type |Description

|`args`
|`<<template-arguments,TArg[]>>`
|Bound arguments for specializations.

|`params`
|`<<template-parameters,TParam[]>>`
|Template parameter list.

|`primary`
|`string`
|Primary template ID for partial and explicit specializations.

|`requires`
|`string`
|The requires-clause for the template parameter list, if any.

|===

