# include/mrdocs/Config/

Configuration types: the public, user-facing settings of a documentation run
(reference directories, the generated settings struct, and related types).
The schema-driven portions are generated at build time from
`src/mrdocs/ConfigOptions.json`.
